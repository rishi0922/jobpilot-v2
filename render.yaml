services:
  - type: web
    name: jobpilot-scraper
    runtime: docker
    rootDir: scripts
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: SCRAPER_API_KEY
        sync: false
      - key: NEXT_APP_URL
        sync: false
